# Sample data

Drop a sample `SizeScope-sample-report.csv` here (generated by `Get-FolderSizeReport.ps1`
against a small test folder) so first-time users have something to drag into
SizeScope without needing to run the discovery script themselves first.

Not yet included in this initial publish — see the main tool README for details.
